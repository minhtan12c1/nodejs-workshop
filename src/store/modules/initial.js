
const state = {
  versionPassed: false,
  versionChecked: false,
};

// getters
const getters = {
  versionPassed: states => states.versionPassed,
  versionChecked: states => states.versionChecked,
};

const actions = {
}

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations,
};
